<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.harmonycloud.dao.ci.SonarConfigMapper">
    <resultMap id="BaseResultMap" type="com.harmonycloud.dao.ci.bean.SonarConfig">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="token" jdbcType="VARCHAR" property="token" />
    </resultMap>

    <sql id="selectAllColumns">
        id,name,token
    </sql>

    <insert id="insertSonarConfig" parameterType="com.harmonycloud.dao.ci.bean.SonarConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cicd_sonar_config(
        name,
        token
        )
        VALUES(
        #{name},
        #{token}
        )
    </insert>

    <select id="queryByAll" resultMap="BaseResultMap">
        SELECT <include refid="selectAllColumns"/> FROM cicd_sonar_config
    </select>

</mapper>